import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    /* config options here */
    devIndicators: false,
    images: {
        domains: ["i.postimg.cc", "psw…yourprojectid.supabase.co"],
    },
};

export default nextConfig;
